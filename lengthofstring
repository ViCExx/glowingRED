msg = "fixing to board, txt u when I land."

#print("You're message is " + str(len(msg)) + " characters long.")

#let me try something

filter = (" is " + str(len(msg)) + " characters long.")

#print("Your message: " + msg) + print(filter)"

final = ("Your message: " + (msg) + (filter))

print(final)

# all that for me to be in the next 20 seconds and the instructor says...we can just add commas and avoid the str(len(msg)) instead, we would do something like this following example.

msg = "are you a pc gamer?"
print("Your message is", len(msg), "characters long.")